name: Daily News Fetch

on:
  schedule:
    # 7:00 AM Melbourne time = 21:00 UTC (AEST, UTC+10)
    # During AEDT (UTC+11, Oct–Apr): change to 20:00 UTC
    - cron: "0 21 * * *"
  workflow_dispatch:  # allows manual trigger from GitHub website

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch articles
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python fetch_news.py

      - name: Commit and push articles.json
        run: |
          git config user.name "M5Paperboy Bot"
          git config user.email "bot@m5paperboy"
          git add docs/articles.json
          git diff --staged --quiet || git commit -m "Daily news update $(date -u +%Y-%m-%d)"
          git push
